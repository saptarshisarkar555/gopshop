<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".CallingActivity">

    <ImageView
        android:id="@+id/profile_image_calling_Id"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/profile_image"></ImageView>

    <TextView
        android:id="@+id/username_calling"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/txt"
        android:gravity="center"
        android:maxLines="5"
        android:text="userName"
        android:textColor="@color/colorPrimary"
        android:textSize="30sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/txt"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/linear_layout_calling1"
        android:gravity="center"
        android:maxLines="5"
        android:text="Calling..."
        android:layout_marginBottom="20dp"
        android:textColor="@color/colorAccent"
        android:textSize="20sp"
        android:textStyle="bold" />

    <LinearLayout
        android:id="@+id/linear_layout_calling1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="40dp"
        android:gravity="center"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/make_call_id"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_marginRight="60dp"
            android:visibility="gone"
            android:src="@drawable/make_call" />

        <ImageView
            android:id="@+id/cancel_call_id"
            android:layout_width="110dp"
            android:layout_height="105dp"
            android:src="@drawable/cancel_call" />


    </LinearLayout>


</RelativeLayout>